# Architecture Diagrams

Add the following diagram files to this directory:

- `system-architecture.png` — Full platform architecture (frontend → API → R2 → engine)
- `pipeline.png` — AI recommendation pipeline flowchart (INPUT → RETRIEVAL → SCORING → OUTPUT)
- `streaming.png` — Cloudflare R2 streaming architecture

The Mermaid source for the system architecture is in `/assets/system_architecture.mmd`.
